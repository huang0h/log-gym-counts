// old component for displaying count when a bar is hovered over

// import { useState } from "react";

// const HoverFlag = (props) => {
//     const {count, max, className, position} = props
//     return (
//         <div className = {`flag ${className}`} style = {{left: position.x - 100, top: position.y - 100}}>
//             {`${count} out of ${max}`}
//         </div>
//     )
// }

// export default HoverFlag